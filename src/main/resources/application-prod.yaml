server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://postgres16:5432/interview
    username: interview_db_user
    password: d9c7c65f-dfe0-41ec-be4b-a6e7460763fb
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

  flyway:
    enabled: true
    locations: classpath:db/migration

jwt:
  secret: ZGV2LW9ubHktc2VjcmV0LWtleS1mb3ItbXVsYWthdGltLWFwcC0yMDI2
  access-token-expiry: 900
  refresh-token-expiry: 604800

google:
  client-id: ${GOOGLE_CLIENT_ID:xxx}

quiz:
  preview-limit: 3
  guest-session-ttl: 3600

cors:
  allowed-origins: ${CORS_ALLOWED_ORIGINS:https://mulakatim.com,http://localhost:5173,http://localhost:3000}

logging:
  level:
    com.mulakatim: INFO
    org.springframework.security: WARN
